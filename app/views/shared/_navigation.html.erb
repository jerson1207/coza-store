<nav class="fixed top-0 w-full bg-white z-10 py-2">
  <div class="max-w-full mx-4 sm:mx-0 px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <div class="flex-shrink-0 text-xl">
          <span class="font-bold">COZA</span><span class="font-light">STORE</span>
        </div>
        <div class="hidden sm:block sm:ml-6">
          <div class="flex space-x-4">
            <%= link_to 'Home', root_path, class: "text-gray-500 hover:underline py-2 rounded-md text-md font-medium" %>
            <%= link_to 'Shop', products_path, class: "text-gray-500 hover:underline py-2 rounded-md text-md font-medium" %>
          </div>
        </div>
      </div>
      <div class="hidden sm:flex sm:items-center sm:justify-center">
        <% if admin_signed_in? %>
          <%= link_to "Admin", admin_path, class: "text-gray-500 hover:underline py-2 rounded-md text-md font-medium" %>
        <% elsif user_signed_in? %>
          <%= link_to cart_path, class: "text-gray-500 hover:underline py-2 rounded-md text-md font-medium" do %>
            <%= icon 'fa-solid', 'shopping-cart' %>
          <% end %>
            <%= render "shared/profile" %>
          
          
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "text-gray-500 hover:underline py-2 rounded-md text-md font-medium" %>
          <%= link_to "Sign up", new_user_registration_path, class: "ml-4 text-gray-500 hover:underline py-2 rounded-md text-md font-medium" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
